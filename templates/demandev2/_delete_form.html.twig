<form method="post" action="{{ path('app_demandev2_delete', {'id': demandev2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandev2.id) }}">
    <button class="btn">Supprimer</button>
</form>
